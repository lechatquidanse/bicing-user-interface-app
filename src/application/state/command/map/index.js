import * as actions from 'application/state/command/map/actions';
import operation from 'application/state/command/map/operations';
import reducer from 'application/state/command/map/reducers';
import * as types from 'application/state/command/map/types';
import * as selectors from 'application/state/command/map/selectors';

export {
  actions,
  operation,
  selectors,
  types,
};

export default reducer;
